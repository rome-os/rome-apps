formatVersion: 1
id: rome-table
name: RomeTable
icon: assets/icon.svg
version: 0.2.1
description: Database browser for inspecting Rome system and app tables
tagline: "Browse every Rome system and app table in one place."
appRoot: dist

web:
  manifest: web/manifest.json
  displayName: RomeTable
  navLabel: Database

api:
  entry: api/index

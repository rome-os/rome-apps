# Store listing for romeos.cc/store/x-manager. Published as a sidecar: it never
# ships to installed instances and never changes the app's content hash, so
# listing copy can be edited without prompting anyone to upgrade.
#
# Still to fill in per app: category, keywords, long_description, and media
# (screenshots of the running app, shown in the listing's gallery).

title: X Manager
description: "Run your X account: brand voice, posts, replies, and metrics."

# Share card for this listing's link. Without it the listing falls back to the
# site-wide default image.
image: assets/store-card.png
image_alt: "X Manager on the Rome App Store"

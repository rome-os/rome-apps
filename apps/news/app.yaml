formatVersion: 1
id: news
name: News
version: 0.2.2
description: News and Reddit ingestion actions
tagline: "News and Reddit sources pulled into one scheduled feed."
appRoot: dist

web:
  manifest: web/manifest.json
  displayName: News
  navLabel: News

api:
  entry: api/index

db:
  migrations: db/migrations
  tablePrefix: news

actions:
  - actions/reddit
  - actions/reddit-scan
  - actions/daily-news
